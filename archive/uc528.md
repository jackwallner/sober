# Sober — App Store Release Readiness (2026-05-28)

Focus: ensure the app **functions for a real downloader who converts to Bloom+**, and for an **App Store reviewer**. Not an ASO/metadata pass.

## Bottom line
The **in-app side of purchasing is correct and verified.** Whether a real purchase actually
*completes* for a user or reviewer now depends on **external RevenueCat + App Store Connect
configuration that can't be seen from the dev machine** — that is the one remaining gate.
Everything in the repo that could block it is fixed, committed, and pushed (`d6bef44`).

---

## ✅ Fixed, built, and pushed — commit `d6bef44` (origin/main)

| Fix | Why it mattered |
|---|---|
| **Free-Pro backdoor removed from Release.** Paywall `devPlaceholder`'s "Continue (dev)" button calling `setLocalOverride(isPro: true)` is now `#if DEBUG` only; Release shows the normal empty/loading state. | Was a Guideline 3.1.1 rejection vector + revenue leak if RC config ever failed at runtime. |
| **Reviewer dead-end fixed.** App Review notes told the reviewer to use a "developer toggle to enable Bloom+" — that toggle is `#if DEBUG` only and **absent from the Release build Apple gets**. Rewrote notes to walk through the no-charge **sandbox purchase** flow. | Would almost certainly have caused rejection ("can't access Pro features as described"). |
| **Privacy Policy / Support links now live.** They pointed at `jackwallner.github.io/sober/...` which returned **404 — GitHub Pages was never enabled.** Enabled Pages (branch `main`, path `/docs`); `privacy-policy.html` and `support.html` now return **200**. No code change — the app's existing hardcoded URL resolves. | Guaranteed rejection for a subscription app with a dead privacy link. |
| **Widget privacy manifest** added (`SoberWidgets/PrivacyInfo.xcprivacy`, UserDefaults `CA92.1`) and wired into `project.yml`. | The widget reads App Group UserDefaults via `WidgetSnapshotStore`; Apple requires the required-reason API declared. |
| **Version `0.1.0 → 1.0`** (`project.yml` `MARKETING_VERSION`). | Must match the App Store Connect draft version "1.0" so the binary attaches to it. |
| **en-US privacy/support/marketing URLs populated** + `SubscriptionService` apiKey comment clarified. | Required App Store fields; comment previously said "replace before shipping." |

### Verified
- **Release build: SUCCEEDED** (iOS Simulator, `CODE_SIGNING_ALLOWED=NO`, after backdoor excision).
- **Unit tests: SUCCEEDED.**
- PaywallView `#if`/`#endif` balanced (8/8).
- GitHub Pages: `status: built`; privacy + support pages both **HTTP 200**.

### Purchase plumbing (traced end-to-end — correct)
- `purchase()` → applies `CustomerInfo` → `pro` entitlement → `isProSubscriber` → all gates unlock
  (Today / Health / Journal / Achievements / Stats / Garden) and the sheet auto-dismisses.
- Restore Purchases button present and wired.
- Apple 3.1.2 disclosure (full price, trial terms, auto-renew, manage-in-Settings) shown on CTA.
- Terms (Apple standard EULA) + Privacy Policy links on the paywall footer.
- Product IDs: `com.jackwallner.sober.pro.{monthly,yearly,lifetime}` ($4.99/mo, $29.99/yr, $79.99 lifetime; monthly+yearly carry a 1-week free trial). `Sober.storekit` mirrors these for Simulator testing.
- Offering lookup: `offering(identifier: "default") ?? current`.

---

## 🔴 Must confirm before submitting — this decides if anyone can actually buy
No RevenueCat dashboard access and no ASC issuer ID on this machine, so these are **unverified**.
If any is wrong, the paywall shows "Couldn't Load Plans" and **no one can convert**:

1. **RC key is the production public SDK key** for this app — `appl_eTgmJWtWPGZuOHUGMvSEpOOemxA` (`Shared/Services/SubscriptionService.swift:21`).
2. **RC has an offering named `default`** containing the **monthly, yearly, and lifetime** packages, all mapped to the **`pro`** entitlement.
3. **App Store Connect has the 3 IAPs** (`...pro.monthly` / `.yearly` / `.lifetime`) — priced, **"Ready to Submit," and attached to the 1.0 version.** First-time IAPs must be submitted *with* the build or the reviewer sees no products.
4. **Screenshots** uploaded for the required device size, and the **App Privacy** nutrition label filled in.

> To verify directly: the IAP/version state can be queried via the ASC API, but the **issuer ID
> is not stored locally** (searched shell rc files, `~/.config`, `~/.fastlane`, repo scripts/logs —
> not found). The signing keys exist: `~/.appstoreconnect/private_keys/AuthKey_9T82M4AZQ2.p8`
> (+ `~/.private_keys/AuthKey_27M3333KDW.p8`, `AuthKey_AQ7Y7LH4G3.p8`). Provide the issuer ID
> (or run `scripts/asc-ensure-draft-version.sh` with your creds) and the IAP/version/offering
> state can be confirmed automatically.
> ASC app id: `6768869215` · bundle `com.jackwallner.sober` · team `YXG4MP6W39`.

---

## 🟡 Minor copy drift (not blockers)
- **Health free count:** code reveals **5 free** milestones (`HealthView.freeRevealCount = 5`);
  `docs/support.html` says "first 2 are free." App is more generous than stated.
- **Species count:** paywall says "**6 bonsai** species"; support page says "5 tree species (Oak free; 4 Pro)."
- The ASO/metadata pipeline (keywords, ~50 localized listings, `primary_category`/`secondary_category`
  files) was left untouched per scope; the audit flagged the category `.txt` files empty — verify those
  are set in ASC (likely managed by the astro pipeline, not the fastlane files).

---

## Notes
- The local terminal stdout rendering went intermittently blank during the session; build / commit /
  push / Pages results were verified via background-task **exit codes** rather than reading stdout —
  all returned success.
- Privacy policy (`docs/privacy-policy.html`) and support page (`docs/support.html`) were reviewed:
  accurate and reviewer-ready (on-device storage, RevenueCat for receipts only, no HealthKit, no
  accounts, cancellation instructions, contact `jackwallner@gmail.com`).
- `docs/index.html`, `docs/privacy-policy.html`, `docs/support.html` have **pre-existing uncommitted
  edits** in the working tree (not made in this pass). Pages currently serves the committed versions
  (which work). Commit + push those if you want the latest copy live.
